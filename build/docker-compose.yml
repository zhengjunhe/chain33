version: '3'

services:
  dplatform:
    build:
      context: .

  chain32:
    build:
      context: .
    command: ["/root/dplatform", "-f", "/root/dplatform-solo.toml"]

  chain31:
    build:
      context: .
    command: ["/root/dplatform", "-f", "/root/dplatform-solo.toml"]

  chain30:
    build:
      context: .
    command: ["/root/dplatform", "-f", "/root/dplatform-solo.toml"]

  chain29:
    build:
      context: .
    command: ["/root/dplatform", "-f", "/root/dplatform-solo.toml"]

  chain28:
    build:
      context: .
    command: ["/root/dplatform", "-f", "/root/dplatform-solo.toml"]
