version: '3'

services:
  autotest:
    build:
      context: .
      dockerfile: Dockerfile-autotest
    container_name: autotest-dplatform

  chain32:
    build:
      context: .
      dockerfile: Dockerfile-autotest
    container_name: autotest-chain32


